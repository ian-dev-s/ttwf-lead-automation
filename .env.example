# Firebase (client-side, prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_FIREBASE_API_KEY="demo-key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="demo-ttwf-leads.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="demo-ttwf-leads"
NEXT_PUBLIC_USE_EMULATORS="true"

# Firebase Admin (server-side)
# For production: set real service account credentials
FIREBASE_PROJECT_ID="demo-ttwf-leads"
FIREBASE_CLIENT_EMAIL=""
FIREBASE_PRIVATE_KEY=""

# Emulator hosts (set these for local development; remove for production)
FIRESTORE_EMULATOR_HOST="localhost:8080"
FIREBASE_AUTH_EMULATOR_HOST="localhost:9099"

# Encryption key for AES-256-GCM (32 bytes / 64 hex chars)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Used to encrypt secrets stored in the database (SMTP, IMAP, API keys)
ENCRYPTION_KEY=""

# Redis for Job Queue (optional)
REDIS_URL="redis://localhost:6379"
